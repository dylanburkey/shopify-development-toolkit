---
import Dashboard from '../../layouts/Dashboard.astro';
---

<Dashboard title="Docs: Deployment" activeNav="docs">
  <div class="max-w-3xl space-y-4">
    <p class="text-gray-700">
      This app runs on Cloudflare Workers with D1 for persistence. Wrangler is used for local preview and deployment.
    </p>

    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
      <h2 class="text-lg font-semibold text-gray-900 mb-2">How it works</h2>
      <div class="space-y-2 text-gray-700">
        <p>Local dev uses Astro’s dev server; production deploy uses Wrangler.</p>
        <p>D1 migrations should be applied via Wrangler commands and tracked in version control.</p>
      </div>
    </div>

    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
      <h2 class="text-lg font-semibold text-gray-900 mb-2">Best practices</h2>
      <div class="space-y-2 text-gray-700">
        <p>Use separate environments for staging vs production.</p>
        <p>Monitor errors and performance (Sentry is configured).</p>
      </div>
    </div>
  </div>
</Dashboard>
